## Api do iCondoApp

- Gerar novos migrates e alterações no banco: `npx drizzle-kit generate`.
- Após utilizar o `generate` utilizar o `npx drizzle-kit push`.

Ainda pretendo melhorar mais as estrutura de pastas e como salvar os dados.        